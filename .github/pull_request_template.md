### What this PR does
<!-- One paragraph. What changed, why. Reference the issue if there is one. -->

### How it's been tested
- [ ] Unit tests added / updated (`pytest tests/`)
- [ ] Docker image builds locally (`docker build -t eval-action:dev .`)
- [ ] Smoke-ran against a fixture suite if the change affects the entrypoint
- [ ] PR-comment output reviewed in-message if the comment renderer changed

### Marketplace / breaking-change notes
<!-- If this changes any input/output, the gate ladder, or the comment shape, call it out so the next release notes mention it. -->
